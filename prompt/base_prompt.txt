prompt1 = f"""
        Hãy phân tích ngắn gọn kết quả kinh doanh của {symbol} dựa trên dữ liệu sau:

        {income_df_str}

        Yêu cầu phân tích theo cấu trúc sau:
        1. Điểm nổi bật trong kết quả kinh doanh.
        2. Các chỉ số tài chính quan trọng (doanh thu, lợi nhuận, biên lợi nhuận...).
        3. So sánh xu hướng tăng trưởng so với cùng kỳ năm trước và quý trước (nếu có).
        4. Nhận xét tổng quan (tích cực / tiêu cực / trung lập).
        """

prompt2 = f"""
        Hãy phân tích ngắn gọn Bảng Cân đối kế toán của {symbol} dựa trên dữ liệu sau:

        {balance_df_str}

        Yêu cầu phân tích theo cấu trúc sau:
        1. Tổng quan tài sản:
        - Quy mô tài sản ngắn hạn, dài hạn.
        - Các khoản mục chiếm tỷ trọng lớn (tiền, phải thu, tài sản cố định...).
        2. Cơ cấu nguồn vốn:
        - Quy mô nợ phải trả (ngắn hạn, dài hạn).
        - Quy mô vốn chủ sở hữu (vốn góp, lợi nhuận giữ lại, các quỹ).
        3. Các chỉ số tài chính quan trọng (nếu có dữ liệu):
        - Hệ số nợ / vốn chủ sở hữu (D/E).
        - Hệ số nợ / tổng tài sản.
        - Tỷ trọng tài sản ngắn hạn / tổng tài sản.
        4. Nhận xét tổng quan:
        - Mức độ an toàn tài chính (cơ cấu vốn).
        - Xu hướng thay đổi so với kỳ trước (nếu có dữ liệu so sánh).
        - Đánh giá ngắn gọn: tích cực / tiêu cực / trung lập.
        """

prompt3 = f"""
        Hãy phân tích ngắn gọn Báo cáo Lưu chuyển tiền tệ của {symbol} dựa trên dữ liệu sau:

        {cash_flow_df_str}

        Yêu cầu phân tích theo cấu trúc sau:
        1. Hoạt động kinh doanh (Operating cash flow):
        - Tiền thuần từ HĐKD trước thay đổi vốn lưu động.
        - Các biến động chính: hàng tồn kho, phải thu, phải trả.
        - Lưu chuyển tiền thuần từ HĐKD.
        2. Hoạt động đầu tư (Investing cash flow):
        - Mức chi đầu tư TSCĐ, tài sản dài hạn.
        - Các khoản thu từ thanh lý, bán đầu tư, cổ tức nhận được.
        - Lưu chuyển từ HĐĐT.
        3. Hoạt động tài chính (Financing cash flow):
        - Các dòng tiền chính: vay nợ, trả nợ, cổ tức trả cổ đông.
        - Lưu chuyển từ HĐTC.
        4. Tiền và tương đương tiền:
        - Lưu chuyển tiền thuần trong kỳ.
        - Số dư đầu kỳ và cuối kỳ.
        5. Nhận xét tổng quan:
        - Hoạt động nào tạo/tiêu hao tiền mặt lớn nhất.
        - Xu hướng an toàn thanh khoản.
        - Đánh giá ngắn gọn (tích cực / tiêu cực / trung lập).
        """